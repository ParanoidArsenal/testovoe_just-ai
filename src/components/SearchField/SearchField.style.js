import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    textField: {
        width: '100%',
        borderRadius: '8px',
        background: 'white',
        border: 'none',
    },
    input: {
        padding: '5px 0',
    }
}));